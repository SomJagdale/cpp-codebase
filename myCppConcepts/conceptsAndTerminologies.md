Return Value Optimization (RVO) or Named Return Value Optimization (NRVO) might optimize
What is the correct way to free a 2D array created by malloc? Loop through rows and free each, then free main pointer
What is the primary purpose of the virtual keyword for a member function in C++? - c) To enable runtime polymorphism
What is a "dangling pointer"? A pointer that points to a memory location that has been deallocated.
What is the effect of declaring a class as abstract? It prevents direct instantiation of the class.
What is the purpose of the volatile keyword in C++? To indicate that a variable's value can be changed by hardware, another thread, or an interrupt.
In C++11, what is the primary purpose of std::move? To cast an lvalue to an rvalue reference, enabling move semantics.
std::unordered_map (hash table) provides average O(1) complexity for these operations.std::map (balanced binary search tree) has O(log N) for all three.
What is a "forward declaration" in C++? Declaring a class, function, or variable before its full definition.
